import { SchemaDefinition, Document, Model } from 'mongoose';
import { ZodObject, z, ZodSchema } from 'zod';

/**
 * Converts a Zod schema to a Mongoose SchemaDefinition.
 * Handles basic types and references between models.
 * @param zodSchema - The Zod schema to convert.
 * @param relationshipMappings - Optional mapping of field names to referenced model names.
 * @returns A Mongoose SchemaDefinition.
 */
declare function zodToMongoose(zodSchema: ZodObject<any>, relationshipMappings?: Record<string, string>): SchemaDefinition;
/**
 * Creates a Mongoose model from a Zod schema.
 * Checks if the model already exists to prevent OverwriteModelError.
 * @param modelName - The name of the model.
 * @param zodSchema - The Zod schema to convert.
 * @param relationshipMappings - Optional mapping of field names to referenced model names.
 * @returns A Mongoose model.
 */
declare function createMongooseModel<T extends Document>(modelName: string, zodSchema: ZodObject<any>, relationshipMappings?: Record<string, string>): Model<T>;
/**
 * Validates and sanitizes data using the provided Zod schema.
 * Populates default values as defined in the schema.
 *
 * @param zodSchema - The Zod schema to validate against.
 * @param data - The input data to validate.
 * @param options - Optional configuration for validation behavior.
 * @param options.partial - If true, makes all fields optional for partial validation (useful for updates).
 * @returns The validated and sanitized object.
 * @throws An error if validation fails.
 */
declare function zodToObject<T>(zodSchema: z.ZodType<T>, data: any, options?: {
    partial?: boolean;
}): T;

declare function generatePrismaModel(modelName: string, zodSchema: ZodSchema<any>, schemaToModelName: Map<ZodSchema<any>, string>, modelNameToRelations: Map<string, any>, modelNameToSchema: Map<string, ZodSchema<any>>): string;
declare function generatePrismaEnums(): string;
declare function zodToPrisma(models: {
    name: string;
    schema: ZodSchema<any>;
}[], schemaToModelName: Map<ZodSchema<any>, string>, modelNameToSchema: Map<string, ZodSchema<any>>): string;
declare function handleRelations(modelString: string, schema: z.ZodObject<any>): string;

export { createMongooseModel, generatePrismaEnums, generatePrismaModel, handleRelations, zodToMongoose, zodToObject, zodToPrisma };
